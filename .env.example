# Copiá este archivo a .env para desarrollo local

SECRET_KEY=cambia-esto-por-una-clave-secreta-larga-y-random
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Dejar vacío en desarrollo (usa SQLite automáticamente).
# En Railway, Railway inyecta esta variable solo al agregar el plugin de Postgres.
DATABASE_URL=

TIME_ZONE=America/Argentina/Buenos_Aires
